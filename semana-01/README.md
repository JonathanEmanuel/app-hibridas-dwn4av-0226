# GIT
1.Inicializamos el repositorio
``` bash
    git init
```
2.Configurar el usuario
``` bash
    git config user.name "jonathan Cruz"
    git config user.email hola@jonathancruz.com.ar"
```
3.Agregamos los archivos
``` bash
    git add .
```